import "./Header.css";

//This component displays the header at the top-left of the screen
function Header(): JSX.Element {
    return (
        <div className="Header fw-bold">
		MOVIE  TOWN
        </div>
    );
}

export default Header;
